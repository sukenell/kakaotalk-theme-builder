{
  "name": "kakaotalk-theme-builder",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "env": "node scripts/env-config.mjs",
    "test": "node --test",
    "test:a11y": "playwright test",
    "test:all": "npm test && npm run test:a11y",
    "serve": "node scripts/serve.mjs",
    "templates": "node scripts/build-template-manifest.mjs"
  },
  "devDependencies": {
    "@axe-core/playwright": "^4.13.0",
    "@playwright/test": "^1.62.1"
  }
}
